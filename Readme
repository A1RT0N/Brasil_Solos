# Welcome to SUCN project from UNESCO 🇺🇳




* Requeriments to run: npm, node, python3
* Install App Expo Go in Play Store.

## Get started

1. Install dependencies

   ```bash
   npm install
   npm install expo
   ```

2. Start the app in your smartphone with Expo Go

   ```bash
    npx expo start 
   ```
   or if you want to run in web

   ```bash
    npm run web
   ```

In the output, you'll find options to open the app in a
[Expo Go](https://expo.dev/go), a limited sandbox for trying out app development with Expo. Use the app to scan the QR-Code and run it in your smartphone.

## Issues

In some cases while running in web, run in terminal: npx expo install react-native-web react-dom @expo/metro-runtime 
